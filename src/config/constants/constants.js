export const passwordHelperText =
  "The password has to be at least at least 1 letter, 1 special symbol, 1 number, contain more than 8 and less 35 characters";

export const amountGetProducts = 12;

export const fetchProductUrl = `/products?offset=0&limit=${amountGetProducts}`;
